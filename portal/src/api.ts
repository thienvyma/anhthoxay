/**
 * Portal API Client
 *
 * Re-exports from api/ modules for backward compatibility
 *
 * **Feature: code-refactoring**
 * **Requirements: 2.4**
 */

// Re-export everything from the api folder
export * from './api/index';

// Re-export default
export { default } from './api/index';
